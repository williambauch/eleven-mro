CREATE TABLE `control_split_assignment` (
  `assignment_id` VARCHAR,
  `employee_info` VARCHAR,
  `hours_origin` VARCHAR,
  `hours_split` VARCHAR,
  `skill_info` VARCHAR,
  `status_info` VARCHAR,
  `task_info` VARCHAR
);
