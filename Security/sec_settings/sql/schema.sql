CREATE TABLE `sec_settings` (
  `auth_sn_fb` VARCHAR,
  `auth_sn_fb_app_id` VARCHAR,
  `auth_sn_fb_secret` VARCHAR,
  `auth_sn_google` VARCHAR,
  `auth_sn_google_client_id` VARCHAR,
  `auth_sn_google_secret` VARCHAR,
  `auth_sn_position` VARCHAR,
  `auth_sn_x` VARCHAR,
  `auth_sn_x_key` VARCHAR,
  `auth_sn_x_secret` VARCHAR,
  `brute_force` VARCHAR,
  `brute_force_attempts` VARCHAR,
  `brute_force_time_block` VARCHAR,
  `captcha` VARCHAR,
  `cookie_expiration_time` VARCHAR,
  `enable_2fa` VARCHAR,
  `enable_2fa_api` VARCHAR,
  `enable_2fa_api_type` VARCHAR,
  `enable_2fa_expiration_time` VARCHAR,
  `enable_2fa_mode` VARCHAR,
  `group_administrator` VARCHAR,
  `group_default` VARCHAR,
  `language` VARCHAR,
  `login_mode` VARCHAR,
  `mfa_last_updated` INTEGER,
  `new_users` VARCHAR,
  `password_min` INTEGER,
  `password_strength` VARCHAR,
  `pswd_last_updated` INTEGER,
  `recover_pswd` VARCHAR,
  `remember_me` VARCHAR,
  `req_email_act` VARCHAR,
  `retrieve_password` VARCHAR,
  `send_email_adm` VARCHAR,
  `session_expire` VARCHAR,
  `smtp_api` VARCHAR,
  `smtp_from_email` VARCHAR,
  `smtp_from_name` VARCHAR,
  `smtp_pass` VARCHAR,
  `smtp_port` INTEGER,
  `smtp_security` VARCHAR,
  `smtp_server` VARCHAR,
  `smtp_user` VARCHAR,
  `theme` VARCHAR
);
