<?php

if( {auth_sn_fb} == 'Y' ){
	sc_field_display({auth_sn_fb_app_id}, on);
	sc_field_display({auth_sn_fb_secret}, on);
}else{
	sc_field_display({auth_sn_fb_app_id}, off);
	sc_field_display({auth_sn_fb_secret}, off);
}
