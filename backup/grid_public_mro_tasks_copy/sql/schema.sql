SELECT
   task_id,
   wbs_id,
   task_code,
   task_name,
   task_type,
   is_nrc,
   parent_task_id,
   root_task_id,
   ata_chapter,
   jic_number,
   "zone",
   requires_rii,
   is_ojt_eligible,
   status_code,
   target_start,
   target_end,
   actual_start,
   actual_end,
   created_at,
   updated_at,
   target_duration_hours,
   skill_code,
   is_blocked_material,
   is_blocked_tool,
   is_blocked_labor,
   is_critical_path,
   baseline_start,
   baseline_end,
   amm_reference,
   access_panel,
   estimated_hours,
   instruction_text,
   safety_warnings,
   tools_required,
   project_id
FROM
   public.mro_tasks